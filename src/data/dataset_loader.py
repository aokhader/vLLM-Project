"""
File: dataset_loader.py
Purpose: Load and structure the raw dataset used in the project.

Description:
    - Handles reading metadata, images, and user-item interaction logs.
    - Returns clean DataFrames or Python dictionaries for downstream processing.
    - Acts as the single entry point for loading dataset components
      (text, image paths, user histories).
"""
